# Arrays

¿Que es un Array? 


# Arrays Asociativos

¿Ques es un Array Asociativo?
Es un Array que tiene un valor asociado

# Bucles e Iteracion

Definicion

    WHILE:
    DO WHILE:
    BREAK:
    FOR: Declaramos, inicializamos, iteramos.
    CONTINUE:
    FOR IN: Guarda la Posicion del elemento dentro de la lista en la variable.
    FOR OF: Guarda el Valor del elemento dentro de la lista en la variable.
    LABEL: 

# Funciones

Definicion: Es una porcion de codigo que se guarda en un nombre, que cada vez que la llamamos nos trae el codigo y que no es necesario reescribirlo devuelta.

    Return: Devuelve informacion avisando que se termino la funcion.

    Parametros: